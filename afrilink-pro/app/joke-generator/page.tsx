"use client";\n\nimport { useState } from "react";\nimport { Laugh, RotateCcw, Copy, Check } from "lucide-react";\n\ninterface Joke {\n  id: number;\n  type: string;\n  setup?: string;\n  delivery?: string;\n  joke?: string;\n}\n\nexport default function JokeGenerator() {\n  const [joke, setJoke] = useState<Joke | null>(null);\n  const [loading, setLoading] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n  const [copied, setCopied] = useState(false);\n\n  const fetchJoke = async () => {\n    setLoading(true);\n    setError(null);\n    setCopied(false);\n\n    try {\n      const response = await fetch(\n        \"https://official-joke-api.appspot.com/random_joke\"\n      );\n\n      if (!response.ok) {\n        throw new Error(\"Failed to fetch joke\");\n      }\n\n      const data: Joke = await response.json();\n      setJoke(data);\n    } catch (err) {\n      setError(\n        err instanceof Error ? err.message : \"An error occurred while fetching the joke\"\n      );\n      setJoke(null);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  const copyToClipboard = async () => {\n    if (!joke) return;\n\n    const jokeText =\n      joke.type === \"single\"\n        ? joke.joke || \"\"\n        : `${joke.setup}\\n${joke.delivery}`;\n\n    try {\n      await navigator.clipboard.writeText(jokeText);\n      setCopied(true);\n      setTimeout(() => setCopied(false), 2000);\n    } catch (err) {\n      console.error(\"Failed to copy:\", err);\n    }\n  };\n\n  const jokeText =\n    joke && joke.type === \"single\"\n      ? joke.joke\n      : joke?.setup && joke?.delivery\n        ? `${joke.setup} ${joke.delivery}`\n        : null;\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900 flex items-center justify-center p-4\">\n      <div className=\"w-full max-w-2xl\">\n        {/* Header */}\n        <div className=\"text-center mb-12\">\n          <div className=\"flex items-center justify-center gap-3 mb-4\">\n            <Laugh size={40} className=\"text-yellow-400\" />\n            <h1 className=\"text-5xl font-bold text-white\">Joke Generator</h1>\n          </div>\n          <p className=\"text-lg text-gray-300\">\n            Get a random laugh every time you click\n          </p>\n        </div>\n\n        {/* Main Card */}\n        <div className=\"bg-white/10 backdrop-blur-md border border-white/20 rounded-2xl p-8 shadow-2xl\">\n          {/* Joke Display */}\n          <div className=\"mb-8 min-h-[200px] flex flex-col justify-center\">\n            {loading ? (\n              <div className=\"flex flex-col items-center justify-center gap-4\">\n                <div className=\"animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-yellow-400\"></div>\n                <p className=\"text-gray-300 text-lg\">Loading a funny joke...</p>\n              </div>\n            ) : error ? (\n              <div className=\"bg-red-500/20 border border-red-500/50 rounded-lg p-6 text-center\">\n                <p className=\"text-red-200 font-semibold text-lg\">{error}</p>\n                <p className=\"text-red-100 text-sm mt-2\">\n                  Try again or check your internet connection\n                </p>\n              </div>\n            ) : joke ? (\n              <div className=\"space-y-4\">\n                {joke.type === \"twopart\" && (\n                  <>\n                    <div>\n                      <p className=\"text-gray-400 text-sm font-semibold mb-2\">\n                        Setup:\n                      </p>\n                      <p className=\"text-white text-xl leading-relaxed\">\n                        {joke.setup}\n                      </p>\n                    </div>\n                    <div className=\"border-t border-white/10 pt-4\">\n                      <p className=\"text-gray-400 text-sm font-semibold mb-2\">\n                        Punchline:\n                      </p>\n                      <p className=\"text-yellow-300 text-2xl font-semibold leading-relaxed\">\n                        {joke.delivery}\n                      </p>\n                    </div>\n                  </>\n                ) : (\n                  <div>\n                    <p className=\"text-white text-2xl leading-relaxed font-semibold\">\n                      {joke.joke}\n                    </p>\n                  </div>\n                )}\n                <div className=\"text-gray-400 text-xs mt-4 pt-4 border-t border-white/10\">\n                  Type: {joke.type === \"twopart\" ? \"Two-Part\" : \"Single-Line\"} | ID:\n                  {joke.id}\n                </div>\n              </div>\n            ) : (\n              <div className=\"text-center\">\n                <p className=\"text-gray-300 text-xl mb-4\">\n                  👇 Click the button below to get started!\n                </p>\n                <p className=\"text-gray-400\">Your first laugh is just a click away...</p>\n              </div>\n            )}\n          </div>\n\n          {/* Action Buttons */}\n          <div className=\"flex gap-4 flex-col sm:flex-row\">\n            <button\n              onClick={fetchJoke}\n              disabled={loading}\n              className=\"flex-1 bg-gradient-to-r from-yellow-400 to-orange-500 hover:from-yellow-300 hover:to-orange-400 disabled:from-gray-500 disabled:to-gray-600 text-white font-bold py-4 px-6 rounded-xl transition-all duration-300 transform hover:scale-105 disabled:scale-100 disabled:cursor-not-allowed flex items-center justify-center gap-2 text-lg shadow-lg\"\n            >\n              <RotateCcw size={20} />\n              {loading ? \"Loading...\" : \"Get Joke\"}\n            </button>\n\n            {jokeText && (\n              <button\n                onClick={copyToClipboard}\n                className=\"flex-1 bg-gradient-to-r from-blue-400 to-purple-500 hover:from-blue-300 hover:to-purple-400 text-white font-bold py-4 px-6 rounded-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center gap-2 text-lg shadow-lg\"\n              >\n                {copied ? (\n                  <>\n                    <Check size={20} />\n                    Copied!\n                  </>\n                ) : (\n                  <>\n                    <Copy size={20} />\n                    Copy Joke\n                  </>\n                )}\n              </button>\n            )}\n          </div>\n\n          {/* Info Section */}\n          <div className=\"mt-8 pt-6 border-t border-white/10\">\n            <p className=\"text-gray-400 text-sm text-center\">\n              🤖 Powered by{\" \"}\n              <a\n                href=\"https://official-joke-api.appspot.com/\"\n                target=\"_blank\"\n                rel=\"noopener noreferrer\"\n                className=\"text-yellow-400 hover:text-yellow-300 underline\"\n              >\n                Official Joke API\n              </a>\n            </p>\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n}\n"